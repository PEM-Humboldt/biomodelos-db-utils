title: Fix Metadata

command: models fix-metadata

description: Subcomando para reemplazar metadatos de modelos en la colección Models de BioModelos.

notes: 
  - title: "Prerequisitos"
    description: Es necesario haber realizado la configuración inicial de [Mongo](setup_mongo.md).

  - title: "Parámetros"
    auto_help: true

  - title: "Consideraciones" 
    description: |
        - El subcomando valida que el taxID exista en la colección especies.las especies y los modelos si existan antes de hacer la corrección de los metadatos.
        - Para los argumentos `CSV_FILE` y `OUT_FOLDER` se debe proporcionar la ruta absoluta del archivo CSV y la carpeta donde se escribe el log con posibles errores de la validación.

  - title: "Comportamiento del subcomando"
    description: |
        - El subcomando puede modificar metadatos existentes en la base de datos.
        - En caso de encontrar errores en la estructura del archivo, los indica a través de un log almacenado en la carpeta `OUT_FOLDER`.
        - El subcomando no comandos elimina registros existentes.

  - title: "Validaciones"
    description: |
        - Los taxID de las especies deben existir en la colección Species de la base de datos MongoDB de BioModelos.
        - Los modelID de los modelos deben existir en la colección Models de la base de datos MongoDB de BioModelos.
        - Los metadatos que se van a cargar deben cumplir con el esquema de validación de la colección Models.
        - `CSV_FILE`: El archivo CSV debe existir y ser accesible desde el sistema.
        - `OUT_FOLDER`: La ruta debe ser accesible desde el sistema. 
